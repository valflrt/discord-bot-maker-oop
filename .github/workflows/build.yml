name: build

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Set up git (checkout to main)
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          ref: main
          token: ${{ secrets.GH_TOKEN }}

      - name: Setup git
        run: |
          git config --local user.name valflrt
          git config --local user.email vfleurit@gmail.com

      - name: Build
        run: |
          npm install
          npm run build

      - name: Remove useless files and folders
        run: rm -rf ./src/ ./README.md tsconfig.json ./.github ./.vscode ./.gitignore

      - name: Checkout to `build` branch
        run: |
          git checkout -B build

      - name: Push
        run: |
          git add *
          git stage *
          git commit -m "Automated build"
          git push
